+ приложение поднялось в докере

+/- есть README, но при этом там нет описания доступных методов (+)
+/- есть flyway, но роли и пользователей надо самому создавать 
+/- есть Thymeleaf, но очень некрасиво смотриться фронт (+)
+/- есть свагер, но не настроена авторизация и работа с json (нахуй)
+/- используется scheduler, но нет вынесения настройки сервиса в проперти (+)
- куча лишних зависимостей в помнике и нет вынесения версий в проперти (+)
- нет интерфейсов (+)
- нет транзакций (+)
- нет PUT, DELETE, PATCH (201, 204, 404)
- нет getById (+)
- нет пагинации (+)
- нет вынесения констант (строк, чисел) (+)
- нет ДТО (+)

...